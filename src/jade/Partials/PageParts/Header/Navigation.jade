// BEGIN - Navigation
nav#navbar.navbar.navbar-ph.navbar-fixed-top
  .container-fluid
    .navbar-header
      button.navbar-toggle.collapsed(type='button' data-toggle="subnav" data-target='#navbar-collapse' aria-expanded="false")
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='index.html')
        img(src="img/logo.svg" alt="")

    #navbar-collapse.navbar-collapse(aria-expanded="false")
      ul.nav.navbar-nav.nav-ph(role="navigation")
        li.active: a(data-toggle="ankernav" href="#Section-01") Erste Section
        li: a(data-toggle="ankernav" href="#Section-02") Zweite Section
        li: a(data-toggle="ankernav" href="#") Dritte Section
      //-  
        each val, key in nav
          if page.name === key
            -var listClass = 'active'
          else
            if page.parent === key
              -var listClass = 'active'
            else
              - var listClass = ''
          case key
            when navCase.unternehmen
              li(class=listClass): a(href="#nav-unternehmen" data-toggle="subnav" aria-expanded="false" title=key)= key
                span.nav_icon
                  span.sr-only Öffne navigation
                  span.icon-bar
                  span.icon-bar
                  span.icon-bar
            default
              li(class=listClass): a(href=val title=key)= key
